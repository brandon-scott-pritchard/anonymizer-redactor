# Runtime dependencies. Pinned to major versions that are known to work together.
PyMuPDF>=1.24,<2          # PDF text extraction and true redaction
lxml>=5.0,<7              # OOXML manipulation for DOCX
cryptography>=42,<47      # AES encryption of the mapping key
spacy>=3.7,<4             # offline NER suggestions (proposals only, never applied blind)
pytesseract>=0.3.10,<1    # drives Tesseract, bundled in vendor/ or found on PATH
rapidocr-onnxruntime>=1.3,<2  # OCR fallback: pip-installable, models included,
                          # so scanned PDFs work even with no Tesseract anywhere
Pillow>=10,<12            # image handling for the OCR path
fastapi>=0.110,<1         # web front end's local API server
uvicorn>=0.29,<1          # serves it, bound to 127.0.0.1 only
python-multipart>=0.0.9,<1  # document uploads from the browser
